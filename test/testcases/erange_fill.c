/* Do not edit this file. It is produced from the corresponding .m4 source */
/*
 *  Copyright (C) 2016, Northwestern University and Argonne National Laboratory
 *  See COPYRIGHT notice in top-level directory.
 *
 *  $Id$
 */

/* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
 *
 * This program is only active when macro  is defined.
 * It tests whether the data elemensts is "filled" with "filled values" when
 * their contents (to be read or written) cause NC_ERANGE error.
 *
 * The compile and run commands are given below.
 *
 *    % mpicc -g -o erange_fill.c erange_fill -lpnetcdf
 *
 *    % mpiexec -l -n 1 erange_fill erange_fill.nc
 *
 * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <libgen.h> /* basename() */
#include <pnetcdf.h>

#include <testutils.h>

#define LEN 10




#define text char

#ifndef schar
#define schar signed char
#endif
#ifndef uchar
#define uchar unsigned char
#endif
#ifndef ushort
#define ushort unsigned short
#endif
#ifndef uint
#define uint unsigned int
#endif
#ifndef longlong
#define longlong long long
#endif
#ifndef ulonglong
#define ulonglong unsigned long long
#endif


static
int test_default_fill_mode(char* filename) {
    int err, nerrs=0, ncid, old_mode;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_set_fill(ncid, NC_FILL, &old_mode); CHECK_ERR
    if (old_mode == NC_FILL) {
        printf("Error at %s line %d: expected NC_NOFILL but got NC_FILL\n",__func__,__LINE__);
        nerrs++;
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}


static
int test_default_fill_text(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    text buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_CHAR, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_text_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != NC_FILL_CHAR) {
            printf("Error at %s line %d: expect buf[%d]=%c but got %c\n",
                   __func__,__LINE__,i,(text)NC_FILL_CHAR,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_default_fill_schar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    schar buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_BYTE, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_schar_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != NC_FILL_BYTE) {
            printf("Error at %s line %d: expect buf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,(schar)NC_FILL_BYTE,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_default_fill_uchar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    uchar buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_UBYTE, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_uchar_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != NC_FILL_UBYTE) {
            printf("Error at %s line %d: expect buf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,(uchar)NC_FILL_UBYTE,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_default_fill_short(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    short buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_SHORT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_short_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != NC_FILL_SHORT) {
            printf("Error at %s line %d: expect buf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,(short)NC_FILL_SHORT,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_default_fill_ushort(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    ushort buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_USHORT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_ushort_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != NC_FILL_USHORT) {
            printf("Error at %s line %d: expect buf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,(ushort)NC_FILL_USHORT,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_default_fill_int(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    int buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_INT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_int_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != NC_FILL_INT) {
            printf("Error at %s line %d: expect buf[%d]=%d but got %d\n",
                   __func__,__LINE__,i,(int)NC_FILL_INT,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_default_fill_uint(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    uint buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_UINT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_uint_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != NC_FILL_UINT) {
            printf("Error at %s line %d: expect buf[%d]=%u but got %u\n",
                   __func__,__LINE__,i,(uint)NC_FILL_UINT,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_default_fill_long(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    long buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_LONG, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_long_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != NC_FILL_INT) {
            printf("Error at %s line %d: expect buf[%d]=%ld but got %ld\n",
                   __func__,__LINE__,i,(long)NC_FILL_INT,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_default_fill_float(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    float buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_FLOAT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_float_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != NC_FILL_FLOAT) {
            printf("Error at %s line %d: expect buf[%d]=%g but got %g\n",
                   __func__,__LINE__,i,(float)NC_FILL_FLOAT,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_default_fill_double(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    double buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_DOUBLE, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_double_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != NC_FILL_DOUBLE) {
            printf("Error at %s line %d: expect buf[%d]=%g but got %g\n",
                   __func__,__LINE__,i,(double)NC_FILL_DOUBLE,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_default_fill_longlong(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    longlong buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_INT64, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_longlong_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != NC_FILL_INT64) {
            printf("Error at %s line %d: expect buf[%d]=%lld but got %lld\n",
                   __func__,__LINE__,i,(longlong)NC_FILL_INT64,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_default_fill_ulonglong(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    ulonglong buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_UINT64, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_ulonglong_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != NC_FILL_UINT64) {
            printf("Error at %s line %d: expect buf[%d]=%llu but got %llu\n",
                   __func__,__LINE__,i,(ulonglong)NC_FILL_UINT64,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}



static
int test_user_fill_text(char* filename, text fillv) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    text buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_CHAR, 1, &dimid, &varid); CHECK_ERR
    /* put attribute _FillValue does not automatically enable file mode */
    err = ncmpi_put_att(ncid, varid, "_FillValue", NC_CHAR, 1, &fillv); CHECK_ERR
    /* err = ncmpi_def_var_fill(ncid, varid, 0, &fillv); CHECK_ERR */
    err = ncmpi_def_var_fill(ncid, varid, 0, NULL); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_text_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != fillv) {
            printf("Error at %s line %d: expect buf[%d]=%c but got %c\n",
                   __func__,__LINE__,i,(text)fillv,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_user_fill_schar(char* filename, schar fillv) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    schar buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_BYTE, 1, &dimid, &varid); CHECK_ERR
    /* put attribute _FillValue does not automatically enable file mode */
    err = ncmpi_put_att(ncid, varid, "_FillValue", NC_BYTE, 1, &fillv); CHECK_ERR
    /* err = ncmpi_def_var_fill(ncid, varid, 0, &fillv); CHECK_ERR */
    err = ncmpi_def_var_fill(ncid, varid, 0, NULL); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_schar_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != fillv) {
            printf("Error at %s line %d: expect buf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,(schar)fillv,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_user_fill_uchar(char* filename, uchar fillv) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    uchar buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_UBYTE, 1, &dimid, &varid); CHECK_ERR
    /* put attribute _FillValue does not automatically enable file mode */
    err = ncmpi_put_att(ncid, varid, "_FillValue", NC_UBYTE, 1, &fillv); CHECK_ERR
    /* err = ncmpi_def_var_fill(ncid, varid, 0, &fillv); CHECK_ERR */
    err = ncmpi_def_var_fill(ncid, varid, 0, NULL); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_uchar_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != fillv) {
            printf("Error at %s line %d: expect buf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,(uchar)fillv,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_user_fill_short(char* filename, short fillv) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    short buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_SHORT, 1, &dimid, &varid); CHECK_ERR
    /* put attribute _FillValue does not automatically enable file mode */
    err = ncmpi_put_att(ncid, varid, "_FillValue", NC_SHORT, 1, &fillv); CHECK_ERR
    /* err = ncmpi_def_var_fill(ncid, varid, 0, &fillv); CHECK_ERR */
    err = ncmpi_def_var_fill(ncid, varid, 0, NULL); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_short_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != fillv) {
            printf("Error at %s line %d: expect buf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,(short)fillv,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_user_fill_ushort(char* filename, ushort fillv) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    ushort buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_USHORT, 1, &dimid, &varid); CHECK_ERR
    /* put attribute _FillValue does not automatically enable file mode */
    err = ncmpi_put_att(ncid, varid, "_FillValue", NC_USHORT, 1, &fillv); CHECK_ERR
    /* err = ncmpi_def_var_fill(ncid, varid, 0, &fillv); CHECK_ERR */
    err = ncmpi_def_var_fill(ncid, varid, 0, NULL); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_ushort_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != fillv) {
            printf("Error at %s line %d: expect buf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,(ushort)fillv,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_user_fill_int(char* filename, int fillv) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    int buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_INT, 1, &dimid, &varid); CHECK_ERR
    /* put attribute _FillValue does not automatically enable file mode */
    err = ncmpi_put_att(ncid, varid, "_FillValue", NC_INT, 1, &fillv); CHECK_ERR
    /* err = ncmpi_def_var_fill(ncid, varid, 0, &fillv); CHECK_ERR */
    err = ncmpi_def_var_fill(ncid, varid, 0, NULL); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_int_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != fillv) {
            printf("Error at %s line %d: expect buf[%d]=%d but got %d\n",
                   __func__,__LINE__,i,(int)fillv,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_user_fill_uint(char* filename, uint fillv) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    uint buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_UINT, 1, &dimid, &varid); CHECK_ERR
    /* put attribute _FillValue does not automatically enable file mode */
    err = ncmpi_put_att(ncid, varid, "_FillValue", NC_UINT, 1, &fillv); CHECK_ERR
    /* err = ncmpi_def_var_fill(ncid, varid, 0, &fillv); CHECK_ERR */
    err = ncmpi_def_var_fill(ncid, varid, 0, NULL); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_uint_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != fillv) {
            printf("Error at %s line %d: expect buf[%d]=%u but got %u\n",
                   __func__,__LINE__,i,(uint)fillv,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_user_fill_long(char* filename, long fillv) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    long buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_LONG, 1, &dimid, &varid); CHECK_ERR
    /* put attribute _FillValue does not automatically enable file mode */
    err = ncmpi_put_att(ncid, varid, "_FillValue", NC_LONG, 1, &fillv); CHECK_ERR
    /* err = ncmpi_def_var_fill(ncid, varid, 0, &fillv); CHECK_ERR */
    err = ncmpi_def_var_fill(ncid, varid, 0, NULL); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_long_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != fillv) {
            printf("Error at %s line %d: expect buf[%d]=%ld but got %ld\n",
                   __func__,__LINE__,i,(long)fillv,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_user_fill_float(char* filename, float fillv) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    float buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_FLOAT, 1, &dimid, &varid); CHECK_ERR
    /* put attribute _FillValue does not automatically enable file mode */
    err = ncmpi_put_att(ncid, varid, "_FillValue", NC_FLOAT, 1, &fillv); CHECK_ERR
    /* err = ncmpi_def_var_fill(ncid, varid, 0, &fillv); CHECK_ERR */
    err = ncmpi_def_var_fill(ncid, varid, 0, NULL); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_float_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != fillv) {
            printf("Error at %s line %d: expect buf[%d]=%g but got %g\n",
                   __func__,__LINE__,i,(float)fillv,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_user_fill_double(char* filename, double fillv) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    double buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_DOUBLE, 1, &dimid, &varid); CHECK_ERR
    /* put attribute _FillValue does not automatically enable file mode */
    err = ncmpi_put_att(ncid, varid, "_FillValue", NC_DOUBLE, 1, &fillv); CHECK_ERR
    /* err = ncmpi_def_var_fill(ncid, varid, 0, &fillv); CHECK_ERR */
    err = ncmpi_def_var_fill(ncid, varid, 0, NULL); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_double_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != fillv) {
            printf("Error at %s line %d: expect buf[%d]=%g but got %g\n",
                   __func__,__LINE__,i,(double)fillv,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_user_fill_longlong(char* filename, longlong fillv) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    longlong buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_INT64, 1, &dimid, &varid); CHECK_ERR
    /* put attribute _FillValue does not automatically enable file mode */
    err = ncmpi_put_att(ncid, varid, "_FillValue", NC_INT64, 1, &fillv); CHECK_ERR
    /* err = ncmpi_def_var_fill(ncid, varid, 0, &fillv); CHECK_ERR */
    err = ncmpi_def_var_fill(ncid, varid, 0, NULL); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_longlong_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != fillv) {
            printf("Error at %s line %d: expect buf[%d]=%lld but got %lld\n",
                   __func__,__LINE__,i,(longlong)fillv,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_user_fill_ulonglong(char* filename, ulonglong fillv) {
    int i, err, nerrs=0, ncid, dimid, omode, varid;
    ulonglong buf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_UINT64, 1, &dimid, &varid); CHECK_ERR
    /* put attribute _FillValue does not automatically enable file mode */
    err = ncmpi_put_att(ncid, varid, "_FillValue", NC_UINT64, 1, &fillv); CHECK_ERR
    /* err = ncmpi_def_var_fill(ncid, varid, 0, &fillv); CHECK_ERR */
    err = ncmpi_def_var_fill(ncid, varid, 0, NULL); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR
    err = ncmpi_get_var_ulonglong_all(ncid, varid, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        if (buf[i] != fillv) {
            printf("Error at %s line %d: expect buf[%d]=%llu but got %llu\n",
                   __func__,__LINE__,i,(ulonglong)fillv,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}



static
int test_erange_put_schar_uchar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    schar buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_BYTE, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_BYTE, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_BYTE, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    uchar wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (uchar) 255;
    err = ncmpi_put_var_uchar_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    if (cdf == NC_FORMAT_CDF2) CHECK_ERR
    err = ncmpi_put_var_uchar_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    if (cdf == NC_FORMAT_CDF2) CHECK_ERR

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_schar_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        schar expect = (schar)NC_FILL_BYTE;
        if (cdf != NC_FORMAT_CDF5) expect = (schar)wbuf[i];
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_schar_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        schar expect = fillv;
        if (cdf != NC_FORMAT_CDF5) expect = (schar)wbuf[i];
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_schar_short(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    schar buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_BYTE, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_BYTE, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_BYTE, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    short wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (short) 32767;
    err = ncmpi_put_var_short_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_short_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_schar_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        schar expect = (schar)NC_FILL_BYTE;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_schar_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        schar expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_schar_ushort(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    schar buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_BYTE, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_BYTE, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_BYTE, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    ushort wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (ushort) 65535U;
    err = ncmpi_put_var_ushort_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_ushort_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_schar_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        schar expect = (schar)NC_FILL_BYTE;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_schar_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        schar expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_schar_int(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    schar buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_BYTE, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_BYTE, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_BYTE, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    int wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (int) 2147483647;
    err = ncmpi_put_var_int_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_int_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_schar_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        schar expect = (schar)NC_FILL_BYTE;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_schar_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        schar expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_schar_uint(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    schar buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_BYTE, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_BYTE, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_BYTE, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    uint wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (uint) 4294967295U;
    err = ncmpi_put_var_uint_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_uint_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_schar_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        schar expect = (schar)NC_FILL_BYTE;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_schar_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        schar expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_schar_float(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    schar buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_BYTE, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_BYTE, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_BYTE, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    float wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (float) 3.402823466e+38f;
    err = ncmpi_put_var_float_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_float_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_schar_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        schar expect = (schar)NC_FILL_BYTE;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_schar_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        schar expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_schar_double(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    schar buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_BYTE, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_BYTE, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_BYTE, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    double wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (double) 1.79769313486230e+308;
    err = ncmpi_put_var_double_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_double_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_schar_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        schar expect = (schar)NC_FILL_BYTE;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_schar_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        schar expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_schar_longlong(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    schar buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_BYTE, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_BYTE, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_BYTE, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    longlong wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (longlong) 9223372036854775807LL;
    err = ncmpi_put_var_longlong_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_longlong_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_schar_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        schar expect = (schar)NC_FILL_BYTE;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_schar_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        schar expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_schar_ulonglong(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    schar buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_BYTE, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_BYTE, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_BYTE, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    ulonglong wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (ulonglong) 18446744073709551615ULL;
    err = ncmpi_put_var_ulonglong_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_ulonglong_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_schar_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        schar expect = (schar)NC_FILL_BYTE;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_schar_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        schar expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}

static
int test_erange_put_uchar_schar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    uchar buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_UBYTE, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_UBYTE, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_UBYTE, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    schar wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (schar) -1;
    err = ncmpi_put_var_schar_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_schar_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_uchar_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uchar expect = (uchar)NC_FILL_UBYTE;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_uchar_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uchar expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_uchar_short(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    uchar buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_UBYTE, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_UBYTE, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_UBYTE, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    short wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (short) -1;
    err = ncmpi_put_var_short_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_short_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_uchar_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uchar expect = (uchar)NC_FILL_UBYTE;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_uchar_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uchar expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_uchar_ushort(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    uchar buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_UBYTE, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_UBYTE, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_UBYTE, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    ushort wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (ushort) -1;
    err = ncmpi_put_var_ushort_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_ushort_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_uchar_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uchar expect = (uchar)NC_FILL_UBYTE;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_uchar_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uchar expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_uchar_int(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    uchar buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_UBYTE, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_UBYTE, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_UBYTE, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    int wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (int) -1;
    err = ncmpi_put_var_int_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_int_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_uchar_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uchar expect = (uchar)NC_FILL_UBYTE;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_uchar_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uchar expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_uchar_uint(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    uchar buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_UBYTE, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_UBYTE, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_UBYTE, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    uint wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (uint) -1;
    err = ncmpi_put_var_uint_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_uint_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_uchar_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uchar expect = (uchar)NC_FILL_UBYTE;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_uchar_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uchar expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_uchar_float(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    uchar buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_UBYTE, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_UBYTE, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_UBYTE, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    float wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (float) -1;
    err = ncmpi_put_var_float_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_float_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_uchar_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uchar expect = (uchar)NC_FILL_UBYTE;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_uchar_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uchar expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_uchar_double(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    uchar buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_UBYTE, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_UBYTE, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_UBYTE, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    double wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (double) -1;
    err = ncmpi_put_var_double_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_double_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_uchar_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uchar expect = (uchar)NC_FILL_UBYTE;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_uchar_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uchar expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_uchar_longlong(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    uchar buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_UBYTE, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_UBYTE, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_UBYTE, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    longlong wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (longlong) -1;
    err = ncmpi_put_var_longlong_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_longlong_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_uchar_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uchar expect = (uchar)NC_FILL_UBYTE;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_uchar_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uchar expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_uchar_ulonglong(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    uchar buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_UBYTE, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_UBYTE, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_UBYTE, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    ulonglong wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (ulonglong) -1;
    err = ncmpi_put_var_ulonglong_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_ulonglong_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_uchar_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uchar expect = (uchar)NC_FILL_UBYTE;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_uchar_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uchar expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}

static
int test_erange_put_short_ushort(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    short buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_SHORT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_SHORT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_SHORT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    ushort wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (ushort) 65535U;
    err = ncmpi_put_var_ushort_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_ushort_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_short_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        short expect = (short)NC_FILL_SHORT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_short_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        short expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_short_int(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    short buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_SHORT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_SHORT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_SHORT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    int wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (int) 2147483647;
    err = ncmpi_put_var_int_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_int_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_short_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        short expect = (short)NC_FILL_SHORT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_short_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        short expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_short_uint(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    short buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_SHORT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_SHORT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_SHORT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    uint wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (uint) 4294967295U;
    err = ncmpi_put_var_uint_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_uint_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_short_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        short expect = (short)NC_FILL_SHORT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_short_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        short expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_short_float(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    short buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_SHORT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_SHORT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_SHORT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    float wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (float) 3.402823466e+38f;
    err = ncmpi_put_var_float_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_float_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_short_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        short expect = (short)NC_FILL_SHORT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_short_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        short expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_short_double(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    short buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_SHORT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_SHORT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_SHORT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    double wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (double) 1.79769313486230e+308;
    err = ncmpi_put_var_double_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_double_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_short_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        short expect = (short)NC_FILL_SHORT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_short_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        short expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_short_longlong(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    short buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_SHORT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_SHORT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_SHORT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    longlong wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (longlong) 9223372036854775807LL;
    err = ncmpi_put_var_longlong_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_longlong_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_short_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        short expect = (short)NC_FILL_SHORT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_short_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        short expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_short_ulonglong(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    short buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_SHORT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_SHORT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_SHORT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    ulonglong wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (ulonglong) 18446744073709551615ULL;
    err = ncmpi_put_var_ulonglong_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_ulonglong_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_short_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        short expect = (short)NC_FILL_SHORT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_short_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        short expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}

static
int test_erange_put_ushort_short(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    ushort buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_USHORT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_USHORT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_USHORT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    short wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (short) -1;
    err = ncmpi_put_var_short_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_short_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_ushort_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        ushort expect = (ushort)NC_FILL_USHORT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_ushort_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        ushort expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_ushort_int(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    ushort buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_USHORT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_USHORT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_USHORT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    int wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (int) -1;
    err = ncmpi_put_var_int_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_int_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_ushort_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        ushort expect = (ushort)NC_FILL_USHORT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_ushort_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        ushort expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_ushort_uint(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    ushort buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_USHORT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_USHORT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_USHORT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    uint wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (uint) -1;
    err = ncmpi_put_var_uint_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_uint_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_ushort_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        ushort expect = (ushort)NC_FILL_USHORT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_ushort_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        ushort expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_ushort_float(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    ushort buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_USHORT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_USHORT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_USHORT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    float wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (float) -1;
    err = ncmpi_put_var_float_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_float_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_ushort_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        ushort expect = (ushort)NC_FILL_USHORT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_ushort_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        ushort expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_ushort_double(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    ushort buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_USHORT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_USHORT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_USHORT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    double wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (double) -1;
    err = ncmpi_put_var_double_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_double_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_ushort_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        ushort expect = (ushort)NC_FILL_USHORT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_ushort_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        ushort expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_ushort_longlong(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    ushort buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_USHORT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_USHORT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_USHORT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    longlong wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (longlong) -1;
    err = ncmpi_put_var_longlong_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_longlong_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_ushort_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        ushort expect = (ushort)NC_FILL_USHORT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_ushort_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        ushort expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_ushort_ulonglong(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    ushort buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_USHORT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_USHORT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_USHORT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    ulonglong wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (ulonglong) -1;
    err = ncmpi_put_var_ulonglong_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_ulonglong_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_ushort_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        ushort expect = (ushort)NC_FILL_USHORT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_ushort_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        ushort expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}

static
int test_erange_put_int_uint(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    int buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_INT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_INT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_INT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    uint wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (uint) 4294967295U;
    err = ncmpi_put_var_uint_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_uint_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_int_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        int expect = (int)NC_FILL_INT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%d but got %d\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_int_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        int expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%d but got %d\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_int_float(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    int buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_INT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_INT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_INT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    float wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (float) 3.402823466e+38f;
    err = ncmpi_put_var_float_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_float_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_int_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        int expect = (int)NC_FILL_INT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%d but got %d\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_int_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        int expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%d but got %d\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_int_double(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    int buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_INT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_INT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_INT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    double wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (double) 1.79769313486230e+308;
    err = ncmpi_put_var_double_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_double_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_int_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        int expect = (int)NC_FILL_INT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%d but got %d\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_int_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        int expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%d but got %d\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_int_longlong(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    int buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_INT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_INT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_INT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    longlong wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (longlong) 9223372036854775807LL;
    err = ncmpi_put_var_longlong_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_longlong_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_int_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        int expect = (int)NC_FILL_INT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%d but got %d\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_int_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        int expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%d but got %d\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_int_ulonglong(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    int buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_INT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_INT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_INT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    ulonglong wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (ulonglong) 18446744073709551615ULL;
    err = ncmpi_put_var_ulonglong_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_ulonglong_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_int_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        int expect = (int)NC_FILL_INT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%d but got %d\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_int_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        int expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%d but got %d\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}

static
int test_erange_put_uint_int(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    uint buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_UINT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_UINT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_UINT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    int wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (int) -1;
    err = ncmpi_put_var_int_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_int_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_uint_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uint expect = (uint)NC_FILL_UINT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%u but got %u\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_uint_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uint expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%u but got %u\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_uint_float(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    uint buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_UINT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_UINT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_UINT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    float wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (float) -1;
    err = ncmpi_put_var_float_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_float_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_uint_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uint expect = (uint)NC_FILL_UINT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%u but got %u\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_uint_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uint expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%u but got %u\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_uint_double(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    uint buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_UINT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_UINT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_UINT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    double wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (double) -1;
    err = ncmpi_put_var_double_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_double_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_uint_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uint expect = (uint)NC_FILL_UINT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%u but got %u\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_uint_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uint expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%u but got %u\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_uint_longlong(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    uint buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_UINT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_UINT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_UINT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    longlong wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (longlong) -1;
    err = ncmpi_put_var_longlong_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_longlong_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_uint_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uint expect = (uint)NC_FILL_UINT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%u but got %u\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_uint_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uint expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%u but got %u\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_put_uint_ulonglong(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    uint buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_UINT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_UINT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_UINT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    ulonglong wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (ulonglong) -1;
    err = ncmpi_put_var_ulonglong_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_ulonglong_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_uint_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uint expect = (uint)NC_FILL_UINT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%u but got %u\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_uint_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        uint expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%u but got %u\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}

static
int test_erange_put_float_double(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid1, varid2, cdf, flag, log_enabled;
    float buf[LEN], fillv=99;
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;
    char hint[MPI_MAX_INFO_VAL];
    MPI_Info infoused;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR

    ncmpi_inq_file_info(ncid, &infoused);
    MPI_Info_get(infoused, "nc_dw_driver", MPI_MAX_INFO_VAL - 1, hint, &flag);
    if (flag && strcasecmp(hint, "enable") == 0){
        log_enabled = 1;
    }
    else{
        log_enabled = 0;
    }
    MPI_Info_free(&infoused);

    err = ncmpi_set_fill(ncid, NC_FILL, NULL); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var1", NC_FLOAT, 1, &dimid, &varid1); CHECK_ERR
    err = ncmpi_def_var(ncid, "var2", NC_FLOAT, 1, &dimid, &varid2); CHECK_ERR
    /* use non-default fill value */
    err = ncmpi_put_att(ncid, varid2, "_FillValue", NC_FLOAT, 1, &fillv); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* put data with ERANGE values */
    double wbuf[LEN];
    for (i=0; i<LEN; i++) wbuf[i] = (double) 1.79769313486230e+308;
    err = ncmpi_put_var_double_all(ncid, varid1, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)
    err = ncmpi_put_var_double_all(ncid, varid2, wbuf);
    if (log_enabled){
        err = ncmpi_sync(ncid);    
    }
    EXP_ERR(NC_ERANGE)

    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var1", &varid1); CHECK_ERR
    err = ncmpi_get_var_float_all(ncid, varid1, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        float expect = (float)NC_FILL_FLOAT;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%g but got %g\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    /* test non-default fill value */
    err = ncmpi_inq_varid(ncid, "var2", &varid2); CHECK_ERR
    err = ncmpi_get_var_float_all(ncid, varid2, buf); CHECK_ERR
    for (i=0; i<LEN; i++) {
        float expect = fillv;
        
        if (buf[i] != expect) {
            printf("Error at %s line %d: expect buf[%d]=%g but got %g\n",
                   __func__,__LINE__,i,expect,buf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}



static
int test_erange_get_uchar_schar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    uchar wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_UBYTE, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (uchar) 255;
    err = ncmpi_put_var_uchar_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    schar rbuf[LEN];
    err = ncmpi_get_var_schar_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        schar expect = (schar)NC_FILL_BYTE;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_short_schar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    short wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_SHORT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (short) 32767;
    err = ncmpi_put_var_short_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    schar rbuf[LEN];
    err = ncmpi_get_var_schar_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        schar expect = (schar)NC_FILL_BYTE;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_ushort_schar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    ushort wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_USHORT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (ushort) 65535U;
    err = ncmpi_put_var_ushort_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    schar rbuf[LEN];
    err = ncmpi_get_var_schar_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        schar expect = (schar)NC_FILL_BYTE;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_int_schar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    int wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_INT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (int) 2147483647;
    err = ncmpi_put_var_int_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    schar rbuf[LEN];
    err = ncmpi_get_var_schar_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        schar expect = (schar)NC_FILL_BYTE;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_uint_schar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    uint wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_UINT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (uint) 4294967295U;
    err = ncmpi_put_var_uint_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    schar rbuf[LEN];
    err = ncmpi_get_var_schar_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        schar expect = (schar)NC_FILL_BYTE;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_float_schar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    float wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_FLOAT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (float) 3.402823466e+38f;
    err = ncmpi_put_var_float_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    schar rbuf[LEN];
    err = ncmpi_get_var_schar_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        schar expect = (schar)NC_FILL_BYTE;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_double_schar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    double wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_DOUBLE, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (double) 1.79769313486230e+308;
    err = ncmpi_put_var_double_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    schar rbuf[LEN];
    err = ncmpi_get_var_schar_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        schar expect = (schar)NC_FILL_BYTE;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_longlong_schar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    longlong wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_INT64, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (longlong) 9223372036854775807LL;
    err = ncmpi_put_var_longlong_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    schar rbuf[LEN];
    err = ncmpi_get_var_schar_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        schar expect = (schar)NC_FILL_BYTE;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_ulonglong_schar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    ulonglong wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_UINT64, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (ulonglong) 18446744073709551615ULL;
    err = ncmpi_put_var_ulonglong_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    schar rbuf[LEN];
    err = ncmpi_get_var_schar_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        schar expect = (schar)NC_FILL_BYTE;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hhd but got %hhd\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}

static
int test_erange_get_schar_uchar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    schar wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_BYTE, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (schar) -1;
    err = ncmpi_put_var_schar_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    uchar rbuf[LEN];
    err = ncmpi_get_var_uchar_all(ncid, varid, rbuf);
    if (cdf == NC_FORMAT_CDF2) CHECK_ERR

    for (i=0; i<LEN; i++) {
        uchar expect = (uchar)NC_FILL_UBYTE;
        if (cdf != NC_FORMAT_CDF5) expect = (uchar)wbuf[i];
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_short_uchar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    short wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_SHORT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (short) -1;
    err = ncmpi_put_var_short_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    uchar rbuf[LEN];
    err = ncmpi_get_var_uchar_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        uchar expect = (uchar)NC_FILL_UBYTE;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_ushort_uchar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    ushort wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_USHORT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (ushort) 65535U;
    err = ncmpi_put_var_ushort_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    uchar rbuf[LEN];
    err = ncmpi_get_var_uchar_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        uchar expect = (uchar)NC_FILL_UBYTE;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_int_uchar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    int wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_INT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (int) -1;
    err = ncmpi_put_var_int_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    uchar rbuf[LEN];
    err = ncmpi_get_var_uchar_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        uchar expect = (uchar)NC_FILL_UBYTE;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_uint_uchar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    uint wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_UINT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (uint) 4294967295U;
    err = ncmpi_put_var_uint_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    uchar rbuf[LEN];
    err = ncmpi_get_var_uchar_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        uchar expect = (uchar)NC_FILL_UBYTE;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_float_uchar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    float wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_FLOAT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (float) -1;
    err = ncmpi_put_var_float_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    uchar rbuf[LEN];
    err = ncmpi_get_var_uchar_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        uchar expect = (uchar)NC_FILL_UBYTE;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_double_uchar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    double wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_DOUBLE, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (double) -1;
    err = ncmpi_put_var_double_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    uchar rbuf[LEN];
    err = ncmpi_get_var_uchar_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        uchar expect = (uchar)NC_FILL_UBYTE;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_longlong_uchar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    longlong wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_INT64, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (longlong) -1;
    err = ncmpi_put_var_longlong_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    uchar rbuf[LEN];
    err = ncmpi_get_var_uchar_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        uchar expect = (uchar)NC_FILL_UBYTE;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_ulonglong_uchar(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    ulonglong wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_UINT64, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (ulonglong) 18446744073709551615ULL;
    err = ncmpi_put_var_ulonglong_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    uchar rbuf[LEN];
    err = ncmpi_get_var_uchar_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        uchar expect = (uchar)NC_FILL_UBYTE;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hhu but got %hhu\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}

static
int test_erange_get_ushort_short(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    ushort wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_USHORT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (ushort) 65535U;
    err = ncmpi_put_var_ushort_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    short rbuf[LEN];
    err = ncmpi_get_var_short_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        short expect = (short)NC_FILL_SHORT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_int_short(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    int wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_INT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (int) 2147483647;
    err = ncmpi_put_var_int_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    short rbuf[LEN];
    err = ncmpi_get_var_short_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        short expect = (short)NC_FILL_SHORT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_uint_short(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    uint wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_UINT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (uint) 4294967295U;
    err = ncmpi_put_var_uint_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    short rbuf[LEN];
    err = ncmpi_get_var_short_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        short expect = (short)NC_FILL_SHORT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_float_short(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    float wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_FLOAT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (float) 3.402823466e+38f;
    err = ncmpi_put_var_float_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    short rbuf[LEN];
    err = ncmpi_get_var_short_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        short expect = (short)NC_FILL_SHORT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_double_short(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    double wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_DOUBLE, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (double) 1.79769313486230e+308;
    err = ncmpi_put_var_double_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    short rbuf[LEN];
    err = ncmpi_get_var_short_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        short expect = (short)NC_FILL_SHORT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_longlong_short(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    longlong wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_INT64, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (longlong) 9223372036854775807LL;
    err = ncmpi_put_var_longlong_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    short rbuf[LEN];
    err = ncmpi_get_var_short_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        short expect = (short)NC_FILL_SHORT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_ulonglong_short(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    ulonglong wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_UINT64, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (ulonglong) 18446744073709551615ULL;
    err = ncmpi_put_var_ulonglong_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    short rbuf[LEN];
    err = ncmpi_get_var_short_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        short expect = (short)NC_FILL_SHORT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hd but got %hd\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}

static
int test_erange_get_short_ushort(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    short wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_SHORT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (short) -1;
    err = ncmpi_put_var_short_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    ushort rbuf[LEN];
    err = ncmpi_get_var_ushort_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        ushort expect = (ushort)NC_FILL_USHORT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_int_ushort(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    int wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_INT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (int) -1;
    err = ncmpi_put_var_int_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    ushort rbuf[LEN];
    err = ncmpi_get_var_ushort_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        ushort expect = (ushort)NC_FILL_USHORT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_uint_ushort(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    uint wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_UINT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (uint) 4294967295U;
    err = ncmpi_put_var_uint_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    ushort rbuf[LEN];
    err = ncmpi_get_var_ushort_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        ushort expect = (ushort)NC_FILL_USHORT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_float_ushort(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    float wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_FLOAT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (float) -1;
    err = ncmpi_put_var_float_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    ushort rbuf[LEN];
    err = ncmpi_get_var_ushort_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        ushort expect = (ushort)NC_FILL_USHORT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_double_ushort(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    double wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_DOUBLE, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (double) -1;
    err = ncmpi_put_var_double_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    ushort rbuf[LEN];
    err = ncmpi_get_var_ushort_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        ushort expect = (ushort)NC_FILL_USHORT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_longlong_ushort(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    longlong wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_INT64, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (longlong) -1;
    err = ncmpi_put_var_longlong_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    ushort rbuf[LEN];
    err = ncmpi_get_var_ushort_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        ushort expect = (ushort)NC_FILL_USHORT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_ulonglong_ushort(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    ulonglong wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_UINT64, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (ulonglong) 18446744073709551615ULL;
    err = ncmpi_put_var_ulonglong_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    ushort rbuf[LEN];
    err = ncmpi_get_var_ushort_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        ushort expect = (ushort)NC_FILL_USHORT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%hu but got %hu\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}

static
int test_erange_get_uint_int(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    uint wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_UINT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (uint) 4294967295U;
    err = ncmpi_put_var_uint_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    int rbuf[LEN];
    err = ncmpi_get_var_int_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        int expect = (int)NC_FILL_INT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%d but got %d\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_float_int(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    float wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_FLOAT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (float) 3.402823466e+38f;
    err = ncmpi_put_var_float_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    int rbuf[LEN];
    err = ncmpi_get_var_int_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        int expect = (int)NC_FILL_INT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%d but got %d\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_double_int(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    double wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_DOUBLE, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (double) 1.79769313486230e+308;
    err = ncmpi_put_var_double_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    int rbuf[LEN];
    err = ncmpi_get_var_int_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        int expect = (int)NC_FILL_INT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%d but got %d\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_longlong_int(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    longlong wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_INT64, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (longlong) 9223372036854775807LL;
    err = ncmpi_put_var_longlong_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    int rbuf[LEN];
    err = ncmpi_get_var_int_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        int expect = (int)NC_FILL_INT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%d but got %d\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_ulonglong_int(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    ulonglong wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_UINT64, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (ulonglong) 18446744073709551615ULL;
    err = ncmpi_put_var_ulonglong_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    int rbuf[LEN];
    err = ncmpi_get_var_int_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        int expect = (int)NC_FILL_INT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%d but got %d\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}

static
int test_erange_get_int_uint(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    int wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_INT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (int) -1;
    err = ncmpi_put_var_int_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    uint rbuf[LEN];
    err = ncmpi_get_var_uint_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        uint expect = (uint)NC_FILL_UINT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%u but got %u\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_float_uint(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    float wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_FLOAT, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (float) -1;
    err = ncmpi_put_var_float_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    uint rbuf[LEN];
    err = ncmpi_get_var_uint_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        uint expect = (uint)NC_FILL_UINT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%u but got %u\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_double_uint(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    double wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_DOUBLE, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (double) -1;
    err = ncmpi_put_var_double_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    uint rbuf[LEN];
    err = ncmpi_get_var_uint_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        uint expect = (uint)NC_FILL_UINT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%u but got %u\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_longlong_uint(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    longlong wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_INT64, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (longlong) -1;
    err = ncmpi_put_var_longlong_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    uint rbuf[LEN];
    err = ncmpi_get_var_uint_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        uint expect = (uint)NC_FILL_UINT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%u but got %u\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}
static
int test_erange_get_ulonglong_uint(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    ulonglong wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_UINT64, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (ulonglong) 18446744073709551615ULL;
    err = ncmpi_put_var_ulonglong_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    uint rbuf[LEN];
    err = ncmpi_get_var_uint_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        uint expect = (uint)NC_FILL_UINT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%u but got %u\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}

static
int test_erange_get_double_float(char* filename) {
    int i, err, nerrs=0, ncid, dimid, omode, varid, cdf;
    double wbuf[LEN];
    MPI_Info info=MPI_INFO_NULL;
    MPI_Comm comm=MPI_COMM_WORLD;

    /* create a new file */
    err = ncmpi_create(comm, filename, NC_CLOBBER, info, &ncid); CHECK_ERR
    err = ncmpi_def_dim(ncid, "X", LEN, &dimid); CHECK_ERR
    err = ncmpi_def_var(ncid, "var", NC_DOUBLE, 1, &dimid, &varid); CHECK_ERR
    err = ncmpi_enddef(ncid); CHECK_ERR

    err = ncmpi_inq_format(ncid, &cdf); CHECK_ERR

    /* write MAX values */
    for (i=0; i<LEN; i++)
        wbuf[i] = (double) 1.79769313486230e+308;
    err = ncmpi_put_var_double_all(ncid, varid, wbuf); CHECK_ERR
    err = ncmpi_close(ncid); CHECK_ERR

    /* reopen the file and check the contents of variable */
    omode = NC_NOWRITE;
    err = ncmpi_open(comm, filename, omode, info, &ncid); CHECK_ERR
    err = ncmpi_inq_varid(ncid, "var", &varid); CHECK_ERR

    /* get data with ERANGE values */
    float rbuf[LEN];
    err = ncmpi_get_var_float_all(ncid, varid, rbuf);
    EXP_ERR(NC_ERANGE)

    for (i=0; i<LEN; i++) {
        float expect = (float)NC_FILL_FLOAT;
        
        if (rbuf[i] != expect) {
            printf("Error at %s line %d: expect rbuf[%d]=%g but got %g\n",
                   __func__,__LINE__,i,expect,rbuf[i]);
            nerrs++;
        }
    }
    err = ncmpi_close(ncid); CHECK_ERR
    return nerrs;
}


int main(int argc, char** argv) {
    char filename[256];
    int err, nerrs=0, rank, fillv;

    MPI_Init(&argc, &argv);
    MPI_Comm_rank(MPI_COMM_WORLD, &rank);

    if (argc > 2) {
        if (!rank) printf("Usage: %s [filename]\n",argv[0]);
        MPI_Finalize();
        return 1;
    }
    if (argc == 2) snprintf(filename, 256, "%s", argv[1]);
    else           strcpy(filename, "testfile.nc");
    MPI_Bcast(filename, 256, MPI_CHAR, 0, MPI_COMM_WORLD);

    if (rank == 0) {
        char *cmd_str = (char*)malloc(strlen(argv[0]) + 256);
        sprintf(cmd_str, "*** TESTING C   %s for checking for type conflict ", basename(argv[0]));
        printf("%-66s ------ ", cmd_str); fflush(stdout);
        free(cmd_str);
    }

    /*---- CDF-2 format -----------------------------------------------------*/
    /* ncmpi_set_default_format(NC_FORMAT_CLASSIC, NULL); */
    ncmpi_set_default_format(NC_FORMAT_CDF2, NULL);

    nerrs += test_default_fill_mode(filename);

    
    nerrs += test_default_fill_text(filename);
    nerrs += test_default_fill_schar(filename);
    nerrs += test_default_fill_short(filename);
    nerrs += test_default_fill_int(filename);
    nerrs += test_default_fill_long(filename);
    nerrs += test_default_fill_float(filename);
    nerrs += test_default_fill_double(filename);

    fillv=99;
    
    nerrs += test_user_fill_text(filename, (text)fillv);
    nerrs += test_user_fill_schar(filename, (schar)fillv);
    nerrs += test_user_fill_short(filename, (short)fillv);
    nerrs += test_user_fill_int(filename, (int)fillv);
    nerrs += test_user_fill_long(filename, (long)fillv);
    nerrs += test_user_fill_float(filename, (float)fillv);
    nerrs += test_user_fill_double(filename, (double)fillv);

    /* test put ERANGE values */
    
    nerrs += test_erange_put_schar_uchar(filename);
    nerrs += test_erange_put_schar_short(filename);
    nerrs += test_erange_put_schar_int(filename);
    nerrs += test_erange_put_schar_float(filename);
    nerrs += test_erange_put_schar_double(filename);

    
    nerrs += test_erange_put_short_ushort(filename);
    nerrs += test_erange_put_short_int(filename);
    nerrs += test_erange_put_short_uint(filename);
    nerrs += test_erange_put_short_float(filename);
    nerrs += test_erange_put_short_double(filename);

    
    nerrs += test_erange_put_int_float(filename);
    nerrs += test_erange_put_int_double(filename);

    nerrs += test_erange_put_float_double(filename);

    /* test get ERANGE values */
    
    nerrs += test_erange_get_short_schar(filename);
    nerrs += test_erange_get_int_schar(filename);
    nerrs += test_erange_get_float_schar(filename);
    nerrs += test_erange_get_double_schar(filename);

    
    nerrs += test_erange_get_schar_uchar(filename);
    nerrs += test_erange_get_short_uchar(filename);
    nerrs += test_erange_get_int_uchar(filename);
    nerrs += test_erange_get_float_uchar(filename);
    nerrs += test_erange_get_double_uchar(filename);

    
    nerrs += test_erange_get_int_short(filename);
    nerrs += test_erange_get_float_short(filename);
    nerrs += test_erange_get_double_short(filename);

    
    nerrs += test_erange_get_float_int(filename);
    nerrs += test_erange_get_double_int(filename);

    nerrs += test_erange_get_double_float(filename);

    /*---- CDF-5 format -----------------------------------------------------*/
    ncmpi_set_default_format(NC_FORMAT_CDF5, NULL);

    nerrs += test_default_fill_mode(filename);

    
    nerrs += test_default_fill_text(filename);
    nerrs += test_default_fill_schar(filename);
    nerrs += test_default_fill_uchar(filename);
    nerrs += test_default_fill_short(filename);
    nerrs += test_default_fill_ushort(filename);
    nerrs += test_default_fill_int(filename);
    nerrs += test_default_fill_uint(filename);
    nerrs += test_default_fill_long(filename);
    nerrs += test_default_fill_float(filename);
    nerrs += test_default_fill_double(filename);
    nerrs += test_default_fill_longlong(filename);
    nerrs += test_default_fill_ulonglong(filename);

    fillv=99;
    
    nerrs += test_user_fill_text(filename, (text)fillv);
    nerrs += test_user_fill_schar(filename, (schar)fillv);
    nerrs += test_user_fill_uchar(filename, (uchar)fillv);
    nerrs += test_user_fill_short(filename, (short)fillv);
    nerrs += test_user_fill_ushort(filename, (ushort)fillv);
    nerrs += test_user_fill_int(filename, (int)fillv);
    nerrs += test_user_fill_uint(filename, (uint)fillv);
    nerrs += test_user_fill_long(filename, (long)fillv);
    nerrs += test_user_fill_float(filename, (float)fillv);
    nerrs += test_user_fill_double(filename, (double)fillv);
    nerrs += test_user_fill_longlong(filename, (longlong)fillv);
    nerrs += test_user_fill_ulonglong(filename, (ulonglong)fillv);

    /* test put ERANGE values */
    
    nerrs += test_erange_put_schar_uchar(filename);
    nerrs += test_erange_put_schar_short(filename);
    nerrs += test_erange_put_schar_ushort(filename);
    nerrs += test_erange_put_schar_int(filename);
    nerrs += test_erange_put_schar_uint(filename);
    nerrs += test_erange_put_schar_float(filename);
    nerrs += test_erange_put_schar_double(filename);
    nerrs += test_erange_put_schar_longlong(filename);
    nerrs += test_erange_put_schar_ulonglong(filename);

    
    nerrs += test_erange_put_uchar_schar(filename);
    nerrs += test_erange_put_uchar_short(filename);
    nerrs += test_erange_put_uchar_ushort(filename);
    nerrs += test_erange_put_uchar_int(filename);
    nerrs += test_erange_put_uchar_uint(filename);
    nerrs += test_erange_put_uchar_float(filename);
    nerrs += test_erange_put_uchar_double(filename);
    nerrs += test_erange_put_uchar_longlong(filename);
    nerrs += test_erange_put_uchar_ulonglong(filename);

    
    nerrs += test_erange_put_short_ushort(filename);
    nerrs += test_erange_put_short_int(filename);
    nerrs += test_erange_put_short_uint(filename);
    nerrs += test_erange_put_short_float(filename);
    nerrs += test_erange_put_short_double(filename);
    nerrs += test_erange_put_short_longlong(filename);
    nerrs += test_erange_put_short_ulonglong(filename);

    
    nerrs += test_erange_put_ushort_short(filename);
    nerrs += test_erange_put_ushort_int(filename);
    nerrs += test_erange_put_ushort_uint(filename);
    nerrs += test_erange_put_ushort_float(filename);
    nerrs += test_erange_put_ushort_double(filename);
    nerrs += test_erange_put_ushort_longlong(filename);
    nerrs += test_erange_put_ushort_ulonglong(filename);

    
    nerrs += test_erange_put_int_uint(filename);
    nerrs += test_erange_put_int_float(filename);
    nerrs += test_erange_put_int_double(filename);
    nerrs += test_erange_put_int_longlong(filename);
    nerrs += test_erange_put_int_ulonglong(filename);

    
    nerrs += test_erange_put_uint_int(filename);
    nerrs += test_erange_put_uint_float(filename);
    nerrs += test_erange_put_uint_double(filename);
    nerrs += test_erange_put_uint_longlong(filename);
    nerrs += test_erange_put_uint_ulonglong(filename);

    nerrs += test_erange_put_float_double(filename);

    /* test get ERANGE values */
    
    nerrs += test_erange_get_uchar_schar(filename);
    nerrs += test_erange_get_short_schar(filename);
    nerrs += test_erange_get_ushort_schar(filename);
    nerrs += test_erange_get_int_schar(filename);
    nerrs += test_erange_get_uint_schar(filename);
    nerrs += test_erange_get_float_schar(filename);
    nerrs += test_erange_get_double_schar(filename);
    nerrs += test_erange_get_longlong_schar(filename);
    nerrs += test_erange_get_ulonglong_schar(filename);

    
    nerrs += test_erange_get_schar_uchar(filename);
    nerrs += test_erange_get_short_uchar(filename);
    nerrs += test_erange_get_ushort_uchar(filename);
    nerrs += test_erange_get_int_uchar(filename);
    nerrs += test_erange_get_uint_uchar(filename);
    nerrs += test_erange_get_float_uchar(filename);
    nerrs += test_erange_get_double_uchar(filename);
    nerrs += test_erange_get_longlong_uchar(filename);
    nerrs += test_erange_get_ulonglong_uchar(filename);

    
    nerrs += test_erange_get_ushort_short(filename);
    nerrs += test_erange_get_int_short(filename);
    nerrs += test_erange_get_uint_short(filename);
    nerrs += test_erange_get_float_short(filename);
    nerrs += test_erange_get_double_short(filename);
    nerrs += test_erange_get_longlong_short(filename);
    nerrs += test_erange_get_ulonglong_short(filename);

    
    nerrs += test_erange_get_short_ushort(filename);
    nerrs += test_erange_get_int_ushort(filename);
    nerrs += test_erange_get_uint_ushort(filename);
    nerrs += test_erange_get_float_ushort(filename);
    nerrs += test_erange_get_double_ushort(filename);
    nerrs += test_erange_get_longlong_ushort(filename);
    nerrs += test_erange_get_ulonglong_ushort(filename);

    
    nerrs += test_erange_get_uint_int(filename);
    nerrs += test_erange_get_float_int(filename);
    nerrs += test_erange_get_double_int(filename);
    nerrs += test_erange_get_longlong_int(filename);
    nerrs += test_erange_get_ulonglong_int(filename);

    
    nerrs += test_erange_get_int_uint(filename);
    nerrs += test_erange_get_float_uint(filename);
    nerrs += test_erange_get_double_uint(filename);
    nerrs += test_erange_get_longlong_uint(filename);
    nerrs += test_erange_get_ulonglong_uint(filename);

    nerrs += test_erange_get_double_float(filename);

    /* check if PnetCDF freed all internal malloc */
    MPI_Offset malloc_size, sum_size;
    err = ncmpi_inq_malloc_size(&malloc_size);
    if (err == NC_NOERR) {
        MPI_Reduce(&malloc_size, &sum_size, 1, MPI_OFFSET, MPI_SUM, 0, MPI_COMM_WORLD);
        if (rank == 0 && sum_size > 0)
            printf("heap memory allocated by PnetCDF internally has %lld bytes yet to be freed\n",
                   sum_size);
    }

    MPI_Allreduce(MPI_IN_PLACE, &nerrs, 1, MPI_INT, MPI_SUM, MPI_COMM_WORLD);
    if (rank == 0) {
        if (nerrs) printf(FAIL_STR,nerrs);
        else       printf(PASS_STR);
    }

    MPI_Finalize();
    return (nerrs == 0) ? 0 : 1;
}

